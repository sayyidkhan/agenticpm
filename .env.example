# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-api-key-here

# Storage backend: "local" (default) or "blob" (Vercel Blob)
# STORAGE_BACKEND=local

# Required when STORAGE_BACKEND=blob — get from Vercel Blob dashboard
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# Auth credentials as JSON (required on Vercel, optional locally — falls back to user_credentials.json)
# AUTH_USERS={"users":[{"username":"admin","password":"changeme"}]}

# Optional: Development environment
# NODE_ENV=development
